import {
  default as React,
  ComponentProps,
  ButtonHTMLAttributes,
  FC,
  ReactNode,
} from "react";
import {
  Button as MuiButton,
  IconButton,
  styled,
  SvgIcon,
} from "@mui/material";

const Button = MuiButton;

export default Button;